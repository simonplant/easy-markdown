{
  "description": "Bugs and tech debt",
  "items": [
    {
      "id": "BUG-003",
      "title": "CloudAPIProvider: no retry on transient network failure — users get immediate error on brief connectivity drop",
      "intent": "A momentary network blip must not surface as an AI failure — one silent retry is the minimum bar for a production cloud service",
      "description": "CloudAPIProvider maps URLError.timedOut and URLError.networkConnectionLost to EMError.ai(.inferenceTimeout/.cloudUnavailable) with no retry. A brief connectivity drop during generation causes an immediate user-visible failure. A single retry with a 1-2s delay would handle most transient failures transparently.",
      "priority": "must",
      "category": null,
      "steps": [
        "In Sources/EMAI/CloudAPIProvider.swift, identify the URLSession data task call inside generate(). Wrap it in a retry loop: func withRetry<T>(maxAttempts: Int, delay: Duration, _ body: () async throws -> T) async throws -> T — attempt body(), on URLError.networkConnectionLost or URLError.timedOut wait delay then retry, on final attempt rethrow. Use maxAttempts: 2, delay: .seconds(1.5).",
        "Place the retry helper as a private free function in the same file (not a new file — it is used in one place)",
        "Do NOT retry on: URLError.notConnectedToInternet (device is offline — retry is futile), HTTP 4xx errors (client errors — retry is futile), EMError.ai(.cancelled) (user cancelled — do not retry)",
        "Test in EMCloudTests: mock URLSession to fail once with URLError.networkConnectionLost then succeed — verify generate() returns success, not error. Also verify that URLError.notConnectedToInternet is NOT retried (expect immediate failure).",
        "swift build must pass"
      ],
      "acceptanceCriteria": [
        "Single URLError.networkConnectionLost or timedOut error during generation triggers one silent retry after 1.5s",
        "Second consecutive transient failure surfaces as EMError.ai(.cloudUnavailable) to the caller",
        "URLError.notConnectedToInternet is not retried — immediate failure",
        "Unit tests verify retry path and non-retry path"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true,
      "groomingNotes": "User impact: iOS users on cellular frequently hit brief connectivity drops (subway, elevator, building handoff). Today, a 1-second blip during an AI rewrite shows the user an error banner and loses their work-in-progress — they must re-invoke the action manually. One silent retry converts this from a frustrating visible failure to a seamless experience. Scope is intentionally narrow: only two transient error codes are retried; offline and server errors fail immediately. No new UI needed. Priority is correct at 'should' — affects all cloud AI users but is not a blocking defect.",
      "groomedAt": "2026-03-24",
      "lastFailReason": "Implementation failed",
      "lastFailAt": "2026-03-24T00:16:35-07:00",
      "failCount": 1
    },
    {
      "id": "BUG-004",
      "title": "Quick Open (FEAT-016): no macOS keyboard nav, DateFormatter perf, fuzzy score floor",
      "intent": "Quick Open's power-user audience is primarily macOS keyboard users — they cannot navigate results without a mouse today",
      "description": "Three issues found during PR #2 code review: (1) Arrow key and Escape handling via .onKeyPress is wrapped in #if os(iOS) only — macOS users get no keyboard navigation in the Quick Open palette. (2) relativeDate() in QuickOpenResultRow allocates a new DateFormatter per row per render — should be a static let. (3) Fuzzy match score can go deeply negative from gap penalties before being clamped to max(score, 1), making all weak matches rank identically at score 1.",
      "priority": "must",
      "category": "Editor",
      "steps": [],
      "acceptanceCriteria": [
        "macOS: arrow keys move selection and Return opens file in Quick Open palette",
        "macOS: Escape dismisses the Quick Open palette",
        "DateFormatter in QuickOpenResultRow.relativeDate is a static let, not allocated per call",
        "Fuzzy match scores for weak matches are distinguishable — not all clamped to 1"
      ],
      "scope": [],
      "dependsOn": [],
      "status": "todo",
      "passes": false,
      "readyForSprint": true,
      "groomedAt": "2026-03-24",
      "groomingNotes": "Derived from closed PR #2 review. The PR attempted to land FEAT-016 (Quick Open) which was already completed via sprint. Issues found during review: (1) macOS keyboard nav: .onKeyPress modifiers are iOS 17+ only — on macOS, use .onKeyPress on the containing VStack or NSEvent-based key handling. This is the highest-priority fix since Cmd+P users are keyboard-first. (2) DateFormatter allocation: trivial fix, make it `private static let` on QuickOpenResultRow. (3) Fuzzy scoring: the gap penalty loop subtracts distance(from:to:) for each gap with no per-gap cap, so a query matching chars spread across a long filename yields e.g. score=-300 which clamps to 1, same as score=-1. Fix: cap per-gap penalty or use a multiplier that decays rather than going negative. Also noted but not actionable as a bug: QuickOpenResult struct is internal (should be package/private), and the hidden Button hack for Cmd+P on HomeView is fragile but acceptable given SwiftUI limitations on iOS."
    }
  ]
}
